# Requirements and Current-State Matrix

This is the acceptance checklist for the controlled refactor. Requirements are derived from the approved design and the supplied page brief; example figures are illustrative only. Current evidence is from the checked-in Flask source, templates, queries, and baseline tests.

## Requirement matrix

| ID | Requirement | Route | Query/template evidence | Test evidence | Current | Target |
|---|---|---|---|---|---|---|
| L1-A-01 | Capture attention with a clear, contextual introduction to global immunisation and preventable-infection data. | `/` | `views.home`; `templates/home.html` hero names vaccination coverage and preventable infections and offers calls to explore. | `RouteTests.test_all_six_required_pages_render`; `RouteTests.test_home_hero_names_vaccination_coverage_and_preventable_infections` | Met | Retain the accessible landing introduction. |
| L1-A-02 | Identify the site's topics and explain the progression from overview to focused exploration and deeper analysis. | `/` | `home.html` path cards link to both Level 2 explorers and both Level 3 analyses; shared navigation in `base.html` exposes the complete six-page journey. | `RouteTests.test_home_links_to_both_explorers_and_both_analyses` | Met | Retain all four explicit exploration paths. |
| L1-A-03 | Show a dataset snapshot derived from the database (years, countries, antigens, and infection types). | `/` | `queries.get_snapshot` uses `MIN/MAX(YearID)` and counts from `YearDate`, `Country`, `Antigen`, `Infection_Type`; `home.html` snapshot. | `QueryTests.test_snapshot_contains_four_presented_fact_groups`; `RouteTests.test_home_fact_cards_follow_snapshot_database_changes` | Met | Preserve database-derived values and labels. |
| L1-A-04 | Present exactly four clear, contextual database facts relevant to target personas. | `/` | `home.html` contains exactly four `.fact-card` elements backed by `snapshot` fields, with contextual copy that does not hardcode analytical values. | `RouteTests.test_home_renders_exactly_four_database_fact_cards`; `RouteTests.test_home_fact_cards_follow_snapshot_database_changes` | Met | Keep exactly four facts and keep their values database-derived. |
| L1-B-01 | Explain the site's respectful, informative, and unbiased perspective on the social challenge. | `/mission` | `views.mission`; `mission.html` explicitly describes transparent, comparable, respectfully presented data and cautions against presenting association as causation. | `RouteTests.test_mission_presents_perspective_guidance_and_database_content` | Met | Retain the plain, neutral framing. |
| L1-B-02 | Explain how users can use the site, including the orient/focus/deepen exploration guidance. | `/mission` | `mission.html` provides linked Orient, Focus, and Deepen steps spanning the overview, mission, both Level 2 explorers, and both Level 3 analyses. | `RouteTests.test_mission_presents_perspective_guidance_and_database_content` | Met | Preserve the linked three-layer guidance. |
| L1-B-03 | Display target personas with their goals, needs, and supported features from SQLite. | `/mission` | `queries.get_personas` selects `ProjectPersona`; `mission.html` renders every returned role, name, goal, need, and app feature. | `QueryTests.test_mission_data_is_retrieved_from_database`; `RouteTests.test_mission_presents_perspective_guidance_and_database_content` | Met | Keep all persona content database-derived. |
| L1-B-04 | Display the project team with exact names and student-number mapping supplied for submission. | `/mission` | `queries.get_team_members` selects `ProjectTeamMember`; `mission.html` semantically renders returned names, student numbers, and responsibilities; `db.py` and the tracked database still contain placeholder identities. | `RouteTests.test_mission_presents_perspective_guidance_and_database_content` checks every non-placeholder record when present; the placeholder-rejection assertion remains disabled pending exact data. | Blocked: exact names and member-ID/student-number mapping not supplied | Replace placeholders and enable the identity acceptance assertion only after the user supplies exact records. |
| L2-01 | Use parameterized SQL for selection, filtering, sorting/whitelisted ordering, joining, aggregation, and explicit anomaly handling. | `/vaccinations`, `/infections`, `/vaccination-improvement`, `/infection-benchmark` | `queries.py`: `_safe_order`; parameterized query values and limits in all four analytical functions; SQL joins, CTE calculations, aggregation, deterministic ordering, and vaccination `target_status`; captioned server-rendered result tables. | `QueryTests.test_sort_inputs_are_whitelisted`; query behavior, all-sort-mode, limit, weighted-rate, and no-source tests for all four functions; `RouteTests.test_analytical_controls_and_tables_have_accessible_names`; `docs/VERIFICATION.md` six-route/no-JavaScript sweep. | Met | Retain parameterized values, whitelisted ordering, SQL analytical work, and server-rendered GET experiences. |
| L2-02 | Handle missing/invalid values and reported coverage above 100% explicitly without silently capping anomalies. | `/vaccinations` | `get_vaccination_view` uses `COALESCE` fallback from reported coverage to doses/positive target and labels `Reported above 100%`, `Met target`, or unusable input through contextual guidance; template exposes anomaly counts and methodology. | `QueryTests.test_vaccination_values_above_100_are_flagged_not_silently_capped`; threshold/summary query tests; vaccination route tests; `docs/VERIFICATION.md` records 5,415 missing coverage, 153 invalid targets, and 1,312 above-100% records. | Met | Retain explicit fallback/exclusion rules and visible uncapped anomaly labels. |
| L2-A-01 | Provide antigen, year, country, and region controls; country and region filters work independently or together. | `/vaccinations` | `views.vaccinations` validates choices and passes both optional location filters; `vaccinations.html` retains selections in shareable GET controls; the normalized vaccination CTE applies country and region predicates together when supplied. | `RouteTests.test_vaccination_country_filter_shows_target_metrics_and_anomaly`; `RouteTests.test_vaccination_region_filter_limits_country_results`; `RouteTests.test_vaccination_country_and_region_filters_work_together` | Met | Retain independent and combined location filtering. |
| L2-A-02 | Return country results limited to countries meeting at least 90% of the selected antigen target. | `/vaccinations` | `get_vaccination_view` applies `WHERE coverage >= 90` to the country detail result after the coverage fallback is normalized; `vaccinations.html` labels the threshold and renders only those rows. | `QueryTests.test_vaccination_country_rows_only_include_countries_meeting_target`; route tests assert fixture-derived threshold result counts. | Met | Retain the SQL threshold and 500-row safety limit. |
| L2-A-03 | Provide a regional summary with countries with data, countries meeting 90%, and a defined regional coverage statistic. | `/vaccinations` | The regional query groups the normalized antigen/year input and returns usable-data count, target count, and mean usable coverage; `vaccinations.html` renders antigen, year, region, both counts, and average coverage in a captioned table. | `QueryTests.test_vaccination_view_filters_and_summarises_in_sql`; `RouteTests.test_vaccination_page_accepts_filters`; combined-filter route test checks the accessible summary table. | Met | Preserve the regional mean over usable coverage values and accessible table semantics. |
| L2-A-04 | Distinguish reported coverage values above 100% as anomalies in the UI. | `/vaccinations` | Query retains uncapped values and emits `target_status='Reported above 100%'`; page metrics count affected countries, detail badges identify them, and the data-quality note explains the flag. | `QueryTests.test_vaccination_values_above_100_are_flagged_not_silently_capped`; country and region route tests assert the visible anomaly label. | Met | Continue retaining and visibly flagging anomalous values rather than capping them. |
| L2-B-01 | Provide one economy selection, infection type, year, country-name filter, sort field, and direction controls. | `/infections` | `views.infections` validates all controls; `infections.html` retains GET selections and `get_infection_by_economy` applies the country-name search. | `RouteTests.test_infection_page_accepts_filters`; `RouteTests.test_infection_page_shows_an_empty_state_for_a_nonmatching_search` | Met | Retain validated, shareable controls and the page-specific no-match state. |
| L2-B-02 | Show one economy's country-level infection results with infection, country, economy, year, cases, population, and cases per 100,000. | `/infections` | `get_infection_by_economy` projects the rendered country, economy, infection, year, cases, population, and rate fields after joining infection, country, economy, and population tables; `infections.html` presents them in a captioned, scoped-header table. | `QueryTests.test_infection_view_calculates_rate_and_all_economy_summary`; `RouteTests.test_infection_page_accepts_filters` | Met | Keep required columns and exclude non-positive populations explicitly. |
| L2-B-03 | Order country results by the selected resultant column and direction using a whitelist. | `/infections` | `_safe_order` maps country/cases/population/rate to SQL aliases; detail SQL orders by the chosen whitelisted value with a country tie-break. | `QueryTests.test_infection_view_applies_country_search_and_all_sort_modes`; whitelist test. | Met | Retain all four sort modes, direction handling, and deterministic ties. |
| L2-B-04 | Aggregate the selected infection and year across all economic phases using infection, country, economy, and population data. | `/infections` | `summary_sql` groups all economies and calculates weighted cases per 100,000 as `SUM(cases) / SUM(population)`; selected-economy metrics and a captioned all-economy table explain the method. | `QueryTests.test_infection_view_calculates_rate_and_all_economy_summary`; `RouteTests.test_infection_page_accepts_filters` | Met | Preserve weighted aggregation and the methodology note. |
| L3-01 | Design analytical pages around sub-dataset SQL queries and keep Python post-processing minimal. | `/vaccination-improvement`, `/infection-benchmark` | Improvement and benchmark functions use CTEs, joins, calculations, ordering, and limiting in SQL; views only split benchmark global/country rows for presentation. | `QueryTests.test_vaccination_improvement_uses_two_year_datasets`; `QueryTests.test_above_global_query_puts_global_row_first` | Met | Keep analytical logic in SQL and document any unavoidable presentation-only processing. |
| L3-02 | Ensure selected sorting and result limiting are parameterized/whitelisted in analytical results. | `/vaccination-improvement` | `_safe_order` handles improvement/country/endpoint-rate sort keys and both directions; SQL uses a bound `LIMIT ?`; route validates and retains the inclusive 3–50 range. | `QueryTests.test_vaccination_improvement_applies_all_sort_modes_and_limit`; `RouteTests.test_improvement_limit_boundaries_are_accepted_and_retained`; invalid/equal/reversed/malformed route cases. | Met | Retain all whitelisted sort modes, deterministic country ties, and parameterized inclusive limits. |
| L3-A-01 | Provide start year, end year, antigen, and result-count controls. | `/vaccination-improvement` | `vaccination_improvement` strictly parses both years and the count, validates the antigen, requires start before end, and accepts only counts from 3 through 50; the GET form retains valid selections. | `RouteTests.test_improvement_limit_boundaries_are_accepted_and_retained`; `RouteTests.test_improvement_rejects_out_of_range_equal_reversed_and_malformed_values`; reversed-year route test. | Met | Retain strict parsing, visible validation, selected controls, and inclusive 3-50 boundaries. |
| L3-A-02 | Calculate each comparable country's population-based vaccination rate for both selected years. | `/vaccination-improvement` | Endpoint CTEs join `Vaccination` to the same country's `CountryPopulation` row for each selected year, require doses and positive populations, then inner-join the two endpoint datasets. | `QueryTests.test_vaccination_improvement_uses_two_year_datasets`; `QueryTests.test_vaccination_improvement_uses_matching_endpoint_populations_and_exclusions` | Met | Preserve same-year population joins and exclusion of invalid or missing endpoints. |
| L3-A-03 | Return country, start rate, end rate, rate increase, start year, and end year in the improvement result. | `/vaccination-improvement` | The final SQL projection returns country ID/name, bound antigen and endpoint years, both rates, and improvement; the captioned table renders every required value directly from each result row. | `QueryTests.test_vaccination_improvement_uses_two_year_datasets`; `RouteTests.test_improvement_results_render_complete_ranked_comparison_rows` | Met | Keep the complete comparison-row contract and render row values rather than reconstructing them in Jinja. |
| L3-A-04 | Rank countries by population-based improvement, with selected sorting and bounded result count. | `/vaccination-improvement` | SQL calculates and filters positive improvement, applies whitelisted ordering with deterministic country ties, and uses a bound limit; Jinja derives visible ranks from returned order and reports the result count. | `QueryTests.test_vaccination_improvement_applies_all_sort_modes_and_limit`; boundary and ranked-comparison route tests. | Met | Retain positive-only SQL ranking, all four sort modes/directions, parameterized limiting, and loop-order ranks. |
| L3-B-01 | Provide year and infection-type controls. | `/infection-benchmark` | `views.infection_benchmark` validates infection/year; the accessible benchmark template retains both controls in a GET form. | Route render; `RouteTests.test_benchmark_page_includes_accessible_global_row_first`; `RouteTests.test_benchmark_page_explains_when_no_benchmark_is_available` | Met | Retain validated, selected year and infection controls plus the page-specific empty state. |
| L3-B-02 | Calculate the weighted global infection rate as total cases divided by total represented population. | `/infection-benchmark` | `global_rate` computes `SUM(cases)*100000/SUM(population)` over positive-population country rows and suppresses the global branch when that rate is null; the summary and methodology note state the denominator and weighted formula. | `QueryTests.test_benchmark_is_weighted_strict_and_deterministic`; `QueryTests.test_benchmark_returns_no_global_row_without_source_data`; benchmark route tests. | Met | Preserve the weighted calculation, positive-population rule, exact `per 100,000 people` label, and invalid-benchmark suppression. |
| L3-B-03 | Return countries whose infection rate is above the weighted global rate. | `/infection-benchmark` | The country branch cross-joins `global_rate`, filters in SQL with strict `cr.cases_per_100k > gr.cases_per_100k`, and orders descending with a country tie-break; the template reports the returned country count. | `QueryTests.test_benchmark_is_weighted_strict_and_deterministic`; `QueryTests.test_above_global_query_puts_global_row_first`; `RouteTests.test_benchmark_page_includes_accessible_global_row_first` | Met | Retain strict SQL filtering, deterministic ordering, and the database-derived count. |
| L3-B-04 | Display the global benchmark row first, followed by above-global countries sorted by rate. | `/infection-benchmark` | SQL orders the global branch first; the template repeats that row first inside a captioned table with scoped headers and a focusable labelled wrapper, followed by the country rows. | `QueryTests.test_above_global_query_puts_global_row_first`; `RouteTests.test_benchmark_page_includes_accessible_global_row_first`; no-benchmark route test. | Met | Retain global-first table semantics, caption, header scopes, focusable wrapper, and accessible empty state without null-rate output. |

## Route inventory

| Route | View function | Template | Primary requirement groups | Current evidence |
|---|---|---|---|---|
| `/` | `pages.home` | `home.html` | L1-A | Snapshot and latest infection totals loaded from SQL. |
| `/mission` | `pages.mission` | `mission.html` | L1-B | Personas/team loaded from project tables. |
| `/vaccinations` | `pages.vaccinations` | `vaccinations.html` | L2, L2-A | GET filters and SQL detail/summary. |
| `/infections` | `pages.infections` | `infections.html` | L2, L2-B | GET filters, country detail, all-economy summary. |
| `/vaccination-improvement` | `pages.vaccination_improvement` | `vaccination_improvement.html` | L3, L3-A | Two-year SQL ranking and bounded limit. |
| `/infection-benchmark` | `pages.infection_benchmark` | `infection_benchmark.html` | L3, L3-B | Weighted global benchmark and above-global rows. |

## Template inventory

| Template | Route(s) | Relevant evidence |
|---|---|---|
| `base.html` | all | Shared navigation, visible-on-focus skip link, unique main landmark, stylesheet, branded shell, and no script dependency; verified at all three viewport sizes in `docs/VERIFICATION.md`. |
| `home.html` | `/` | Hero/topics, exactly four fact cards, exploration paths, latest infection chart. |
| `mission.html` | `/mission` | Perspective, usage guidance, SQLite personas, team section. |
| `vaccinations.html` | `/vaccinations` | Labelled antigen/year/country/region/sort/direction GET form; scoped, captioned regional and country tables in named focusable overflow regions; anomaly/status and empty states. |
| `infections.html` | `/infections` | Labelled economy/infection/year/search/sort/direction GET form; scoped, captioned detail and all-economy tables in named focusable overflow regions. |
| `vaccination_improvement.html` | `/vaccination-improvement` | Labelled start/end year, antigen, limit, sort/direction GET form and scoped, captioned ranked results in a named focusable overflow region. |
| `infection_benchmark.html` | `/infection-benchmark` | Infection/year form, global benchmark first, above-global country table and methodology. |

## Query inventory

| Query/function | SQL responsibilities | Used by |
|---|---|---|
| `get_snapshot` | Dataset min/max years and entity counts. | `/` |
| `get_latest_infection_totals` | Latest-year infection aggregation and relative widths. | `/` |
| `get_reference_data` | Database-derived choices for all filters. | `/vaccinations`, `/infections`, `/vaccination-improvement`, `/infection-benchmark` |
| `get_personas` / `get_team_members` | Read project persona/team rows. | `/mission` |
| `get_vaccination_view` | Parameterized joins, coverage fallback, anomaly status, sorting, detail limit, regional aggregation. | `/vaccinations` |
| `get_infection_by_economy` | Parameterized joins, population-adjusted rate, country search/sort/limit, all-economy weighted summaries. | `/infections` |
| `get_vaccination_improvements` | Two endpoint CTEs, population rates, improvement calculation, positive-only filter, sort, limit. | `/vaccination-improvement` |
| `get_above_global_infections` | Country rates, weighted global rate, strict-above filter, global-first ordering. | `/infection-benchmark` |

## Test inventory

| Test module | Coverage relevant to matrix |
|---|---|
| `tests/test_queries.py` | Snapshot counts; idempotent project tables; database-derived mission rows; vaccination filters/summary/anomalies; infection rates and economy summary; two-year improvement ranking/limit; global benchmark ordering; sort whitelist/injection safety. |
| `tests/test_routes.py` | All six routes; one-h1/title/navigation/main shell semantics; visible labels for every analytical control; captions, column-header scopes, and named focusable table regions; alert and empty-state headings; Level 1 content; all analytical valid/invalid/empty behavior; branded 404. |

## Cross-page verification evidence

| Experience contract | Implementation evidence | Automated/manual evidence | Status |
|---|---|---|---|
| Semantic page shell and keyboard entry | `base.html` primary navigation, skip link, and `#main-content`; one page-level heading in each route template. | `RouteTests.test_all_six_pages_have_one_named_document_shell`; 375px keyboard sequence in `docs/VERIFICATION.md`. | Met |
| Visible labels, validation alerts, and empty-state headings | Analytical templates use wrapping labels and the shared labelled alert include; each no-data branch has a descriptive heading. | `RouteTests.test_analytical_controls_and_tables_have_accessible_names`, `test_invalid_filters_render_a_labelled_alert`, and `test_empty_results_use_descriptive_headings`; four state sweeps at three sizes. | Met |
| Responsive layouts without page overflow | `styles.css` 1100/860/640 breakpoints collapse grids/forms/cards; narrow navigation and `.table-shell` use local horizontal overflow. | `docs/VERIFICATION.md`: all six routes and four invalid/empty states at 1440x900, 768x1024, and 375x812; computed document/table measurements plus screenshot inspection. | Met |
| Complete accessible data tables | Every analytical table has a caption and scoped headers inside a named `role="region"`, `tabindex="0"` wrapper; no responsive rule removes columns. | Semantic route test plus 375px keyboard/table inspection and 375/768 client-width versus scroll-width measurements. | Met |
| Reduced motion and no JavaScript dependency | Reduced-motion media query disables smooth scrolling and shortens transitions/animations; navigation and GET forms are server-rendered. | Chrome emulation computed `scroll-behavior: auto` and `0.01ms` durations; all route captures reported zero scripts and GET form methods. | Met |

## Audit notes and next checks

- The cross-page Task 9 semantic and visual evidence is recorded in `docs/VERIFICATION.md`; all non-identity matrix rows now have implementation and automated/manual evidence.
- Team identity remains blocked despite placeholder rows existing: exact names and student-number/member-ID mapping were not supplied, so `L1-B-04` remains `Blocked`.
